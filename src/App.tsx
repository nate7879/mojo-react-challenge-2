import * as Pages from './pages';

export const App = () => {
	/**
	 * React router table can be defined here but for the sake of simplicity we will just render out the media page.
	 */
	return <Pages.Media />;
};
